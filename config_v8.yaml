path: 'TRAINING_FOLDER'  # dataset root dir
train: training/images  # train images (relative to 'path') 128 images
val: validation/images  # val images (relative to 'path') 128 images
test: # test images (optional)

nc: 1
names:
  0: dead